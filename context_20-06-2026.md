MASTER CONTEXT PROMPT

You are working on an existing Laravel QR attendance system repository named `Prensensi_QRcode`.

Your task is to modify the existing project without changing the core UI identity, visual style, database meaning, attendance business rules, or existing admin workflow more than necessary.

Before writing code, inspect the existing files and understand the current flow. Pay special attention to:

* `routes/web.php`
* `routes/admin.php`
* `routes/guru.php`
* `routes/kepsek.php`
* `app/Http/Controllers/Admin/TerminalScanController.php`
* `app/Http/Controllers/Admin/DirektoriGuruController.php`
* `app/Http/Controllers/Admin/ProfilPenggunaController.php`
* `app/Http/Controllers/ProfileController.php`
* `app/Services/PresensiService.php`
* `resources/views/admin/scan/index.blade.php`
* `resources/views/admin/scan/edit.blade.php`
* `resources/views/profile/index.blade.php`
* `resources/views/components/sidebar.blade.php`
* `resources/views/layouts/admin.blade.php`
* `public/assets/js/qr-terminal.js`

Main goals:

1. Create a public scanner terminal page at:

   `GET /scan`

   This page must be accessible without the admin sidebar and without the admin layout. It must be designed as a clean public scanner terminal page for a QR/Barcode USB HID scanner in keyboard mode.

   The scanner input must stay active and must process attendance automatically when the scanner sends ENTER.

   The scan result must still be stored through the existing attendance logic, so the scanned attendance data remains visible in the admin attendance records.

   Add a fullscreen option using the browser Fullscreen API. The button must degrade gracefully if fullscreen is unsupported.

2. Convert the previous admin scanner concept:

   `GET /admin/scan`

   This page must no longer contain the real scanner form. It should become a scanner launcher/information page only.

   It should contain:

   * A short scanner terminal explanation.
   * A button that opens or redirects to the public `/scan` page.
   * UI consistent with the existing admin style.
   * No real scan input or scan processing form.

3. Move the teacher card directory from the previous admin scan page to:

   `GET /admin/direktori-guru`

   This page should contain only the teacher directory/card data table and related edit actions.

   Rename routes, links, redirects, sidebar menu items, and route names carefully so the directory feature does not depend on `admin.scan.*` anymore.

4. Add edit profile and change password features to the existing profile page.

   The current profile page is read-only. Add:

   * Edit profile form.
   * Change password form.
   * Secure validation.
   * Current password verification.
   * Password confirmation.
   * Password hashing.
   * Unique username/email validation ignoring the current authenticated user.
   * Safe profile photo update if the project already supports profile photos.

   Do not allow users to self-edit sensitive admin-controlled fields such as role, active status, account status, or teacher identity fields that should remain admin-controlled.

General requirements:

* Keep the existing UI style. Do not redesign the whole application.
* Reuse existing classes, layout patterns, cards, buttons, spacing, and typography where possible.
* Do not change the core attendance rules in `PresensiService` unless absolutely necessary.
* Do not break existing admin, guru, or kepala sekolah login flow.
* Use Laravel best practices: route names, controllers, validation, policies/middleware if needed, transactions where appropriate, CSRF, throttling, secure password handling, and clean redirects.
* Keep the code modular and reusable.
* Avoid duplicated scanner logic.
* Make the UI responsive.
* Avoid introducing unnecessary packages.
* Preserve existing database schema unless a migration is strictly required. Prefer not to add migrations for this task.
* After implementation, provide a list of changed files, route changes, and manual test steps.
